import { consumeProperty, dirtyProperty } from './validator-versions.js';

class PropertyStorageMap {
  #object;
  constructor(object) {
    this.#object = object;
  }
  consume(key) {
    consumeProperty(this.#object, key);
  }
  update(key) {
    dirtyProperty(this.#object, key);
  }
}

export { PropertyStorageMap };
//# sourceMappingURL=property-storage-map.js.map
